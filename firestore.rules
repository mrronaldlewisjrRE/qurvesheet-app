rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /spreadsheets/{document} {
      allow read, write: if true;
    }
    match /users/{user} {
      allow read, write: if request.auth != null && request.auth.uid == user;
    }
  }
}
